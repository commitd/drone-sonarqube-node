pipeline:
  build:
    image: golang:1.8
    environment:
      - GOPATH=/drone
      - GOOS=linux 
      - GOARCH=amd64 
      - CGO_ENABLED=0 
    commands:
      - go build -o drone-sonarqube-node


